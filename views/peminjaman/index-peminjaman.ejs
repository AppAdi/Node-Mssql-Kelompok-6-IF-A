<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MSSQL</title>
</head>
<body>
  <h1>Peminjaman Buku</h1>
  <a href="/create-peminjaman">Buat Peminjaman</a>
  <table>
    <tr>
      <th>No. </th>
      <th>Nama Peminjam</th>
      <th>Nama Buku</th>
      <th>Tanggal Peminjaman</th>
      <th>Tanggal Pengembalian</th>
      <th>Status</th>
      <th>Aksi</th>
    </tr>
    <% listPeminjaman.forEach((item,index)=>{ %>
      <tr>
          <th scope="row"><%= index+1 %></th>
          <td><%= item.nama_lengkap %></td>
          <td><%= item.judul_buku %></td>
          <td><%= item.tanggal_peminjaman.toLocaleString('id-ID').split(" ")[0] %></td>
          <td><%= item.tanggal_pengembalian.toLocaleString('id-ID').split(" ")[0] %></td>
          <td><%= item.status_peminjaman %></td>
          <td>
              <a href="/update-peminjaman/<%= item.id %>" data-id="<%= item.id %>">Edit</a>
              <a href="/delete-peminjaman/<%= item.id %>" data-id="<%= item.id %>">Delete</a>
          </td>
      </tr>
    <%}) %>
  </table>
</body>
</html>